# Envato Homepage V3 Spec and Plan (rev 3)

Reference mockup: `/Users/uxersean/Desktop/final_web_V2.png`.
Codebase: `envato-homepage/` (Next.js 16, Tailwind v4, shadcn). Design tokens, gradients, motion primitives in `src/app/globals.css` are locked.

Discovery sources (most recent first), paths relative to repo root `/Users/uxersean/Desktop/envato_task/envato-homepage/`:
- `/Users/uxersean/Downloads/envato-problem-decision-board.html` (problem-decision-tradeoff thesis board, defines funnel timing 3s/10s/30s/60s, deprioritized list, brand voice tier).
- `../envato_files/envato-research-facts.md` (verified pricing, AI tool list, asset count, MCP details).
- `../envato_files/envato-presentation-framing.md` (panel walkthrough Q&A, which claims are author-extension vs Envato-published).
- Envato Elements public pages, May 2026, for real model lineup per AI tool (cited inline in §5.7).

## 1. Goal

Build `v3` of the marketing homepage at the route `/v3`. Reuse v2's structural patterns; replace section composition, key headlines, and artwork with the new editorial collage direction in the mockup. v2 at `/` stays untouched.

The v3 homepage exists to satisfy the discovery thesis: a universal taste problem, a 25M+ reference library as Envato's structural answer, six chained design decisions (escape the generic, mechanism in one sentence, pick-decode-generate-ship strip, editorial collage brand voice tier, considered-path scrollytelling page already at `/creative-flow`, cinematic below-fold restructure).

## 2. Source-of-truth rule (no exceptions)

Per dimension, exactly one source wins. The implementer must not blend sources within the same dimension.

| # | Dimension | Source of truth | Applies to |
|---|---|---|---|
| 2.1 | Section order, layout, visual composition | Mockup | Every section |
| 2.2 | Headline copy, sub-headline copy, CTA button labels | Mockup | Hero, ThreeRefsFlow, WorldCreativity, RealWork, LovedByBrands, SixTools heading, Pricing heading, FinalCta |
| 2.3 | Pricing tier numbers and feature list copy | Discovery research-facts (Feb 25 2026 launch). Mockup matches discovery, v2 has stale Plus price (A$33) and stale Core features (100M+ assets line). | Pricing tier cards |
| 2.4 | Pricing tier pitch line | v2 (mockup does not show separate pitch lines) | Pricing tier cards |
| 2.5 | Pricing FAQ content | v2, abbreviated to 2 items | FAQ block under Pricing |
| 2.6 | KPI band numbers and labels | v2 (user-authorized deviation from mockup) | KPI pill |
| 2.7 | SixTools card product names and pitches | v2 (mockup uses placeholders, user-authorized deviation) | SixTools cards |
| 2.8 | SixTools card model pill list | Envato public pages May 2026, cited in §5.7 | SixTools cards |
| 2.9 | SixTools card count | Mockup (6 cards, matches v2) | SixTools section |
| 2.10 | RealWork creator handles | Discovery research-facts §0, real campaign creators | RealWork cards |
| 2.11 | LovedByBrands wordmark logo set | v2 (user-authorized deviation from mockup placeholders) | Brand logo row |
| 2.12 | LovedByBrands heading and sub | Mockup | Section copy |
| 2.13 | Funnel timing budget and deprioritized list | Discovery decision board | Hero scope |
| 2.14 | Visual tokens (colors, gradients, fonts, motion utilities) | v2 `globals.css` | Every section |
| 2.15 | Nav and Footer markup | v2 unchanged | Header, footer |
| 2.16 | MCP banner | Removed (user-authorized, matches mockup) | (removed) |
| 2.17 | SixTools card model pill display | Mockup omits pills, v3 keeps them per user direction (rule 2.8 supplies the data) | SixTools cards |
| 2.18 | Pricing tier CTA labels | v2 (mockup labels match v2 exactly: Start with Core, Try Plus 7 Days Free, Go Ultimate) | Pricing tier card buttons |

User-authorized mockup deviations (places where v3 intentionally does not match the mockup) are 2.5 (FAQ kept), 2.6 (KPI labels), 2.7 (SixTools card names and pitches), 2.11 (real brand logo set), 2.16 (MCP removed which actually matches mockup), and 2.17 (model pills kept). The implementer should NOT change the v3 to match the mockup on those dimensions; the user has chosen real-content fidelity over visual mockup fidelity on those specific points.

Section 5 is the canonical source for every copy string and data list. If a dimension above does not appear in §5 anywhere, the implementer must ask before proceeding; nothing else is allowed to be invented.

## 3. Hard rules from memory and AGENTS.md

- No em dashes anywhere. Use commas, periods, colons, parens, or line breaks. Applies to copy strings, comments, this spec.
- Next.js in this repo is the latest. Consult `node_modules/next/dist/docs/` before writing Next-specific APIs.
- No reflexive refactoring of v2. Only touch v2 files if explicitly noted in §11.
- All artwork is generated by Codex CLI. The assistant does not generate images. Two SVG handwritten notes are authored by hand and committed as static files.

## 4. Routing and file strategy

- v3 at `/v3` (new route). v2 stays at `/`.
- Page composition in `src/app/v3/page.tsx`. Imports the v2 `Nav`, the v2 `Footer`, the v3 sections, the existing `Reveal` wrapper.
- v3 sections live in `src/components/sections/v3/`. New files only. Naming: `HeroV3.tsx`, `KpiPill.tsx`, `ThreeRefsFlow.tsx`, `WorldCreativity.tsx`, `RealWork.tsx`, `LearnHow.tsx`, `LovedByBrands.tsx`, `SixTools.tsx`, `PricingV3.tsx`, `FinalCtaV3.tsx`. Ten v3 sections total.
- v3 artwork in `public/v3/`. v2 public assets untouched.

## 5. Section-by-section spec

Every copy string and data list shown below is canonical. Implementer pastes them as written. Numbers in brackets like `[mockup]`, `[discovery]`, `[v2]` mark the source of each item.

**Intentional deviations from mockup**, all user-authorized:
1. KPI band labels use v2's shorter strings, not the mockup's longer ones
2. SixTools card names use real Envato product names (VideoGen, ImageGen, etc), not the mockup's placeholder names (REFERENCE STUDIO, CINEMA STUDIO, etc)
3. SixTools cards display model pills, not bare cards as in the mockup
4. RealWork uses real campaign creator handles, not the mockup's `@frumework` etc placeholders
5. LovedByBrands uses v2's real brand logo set (Miami Dolphins, KPMG, etc), not the mockup's AKQA / MSCHF placeholder wordmarks
6. Pricing keeps an abbreviated 2-item FAQ under the tier cards; mockup omits FAQ
7. MCP banner is removed; matches the mockup (v2 had it; the user confirmed remove)

A reviewer who diffs the mockup against the v3 surface MUST treat the above as expected gaps, not regressions.

### 5.1 Hero (`HeroV3.tsx`)

Background: dark `var(--hero)` (#0a0a0a). One full-bleed collage still, `/v3/hero_v3_main.jpg`, anchored right and bleeding down past the KPI pill. No video on v3. Still hero keeps the page light and matches the mockup composition.

Copy:
- Eyebrow: none
- Headline: `ESCAPE / THE / GENERIC.` [mockup, v2, discovery Chain 01]. "GENERIC." italic, `text-gradient-brand`, same `animate-rise-in` staggered entrance as v2.
- Sub-headline: `Pick a few references. AI generates with your taste in the loop.` [mockup, discovery Chain 02]
- Primary CTA: `Start Free Trial`, links to `#pricing` [mockup, v2]
- Secondary CTA: `How It Works`, links to `/creative-flow` [mockup, v2]
- Handwritten note SVG below the CTAs: `Your taste, not the algorithm.` with a small star ornament [mockup]
- Scroll arrow icon (lucide `ArrowDown`) bottom-left, no label

Layout:
- Two-column on desktop (text left, art right that bleeds across both columns)
- Single column on mobile (art behind text, text takes full width, scrim heavier)
- Min-height `92svh` like v2 hero
- Same scrim stack as v2: left-to-right hero gradient and bottom hero gradient

Funnel role:
- 3 sec test: headline answers "is this for me?"
- 10 sec test: sub-headline alone closes the mechanism. The 4-step strip in §5.3 is below KPI and supports, but the 10s gate is met by the subhead.

### 5.2 KPI pill band (`KpiPill.tsx`)

Background: cream `var(--bg)`. Pill itself: white `var(--paper)`, rounded-full, subtle shadow `shadow-[0_18px_36px_-20px_rgba(0,0,0,0.18)]`, max-width `1100px`, centered, sits visually overlapping the hero bottom edge.

Five cells, evenly spaced, each cell with a small lucide icon (16px) to the left of the KPI number:

| Icon | KPI | Label |
|---|---|---|
| `Files` | `25M+` | Assets [v2] |
| `Users` | `3M+` | Creators [v2] |
| `ShieldCheck` | `100%` | Licensed [v2] |
| `Download` | `Unlimited` | Downloads [v2] |
| `Gem` | `One` | Subscription [v2] |

Typography:
- KPI: `font-display`, 28px desktop / 22px mobile, `tracking-display`, `font-medium`, ink color
- Label: 12px desktop / 11px mobile, mono uppercase tracking 0.2em, `var(--muted-ink)`

KPI labels and values are taken verbatim from v2 `KpiBand.tsx`. The mockup uses slightly different labels (`Assets & templates`, `Commercial license`, `Creative platform`). Per the source-of-truth rule, v2 wins on KPI labels because the mockup labels are not contradicted by discovery; v2 labels match the existing brand surface.

### 5.3 From three references, one creation (`ThreeRefsFlow.tsx`)

Background: dark `var(--hero)`. Section sits below the KPI pill.

Copy:
- Heading (left column): `From three references,` line break `one creation.` [mockup]
- Handwritten lime annotation (left column, below heading): `Your references become the AI's instructions.` with a curved lime arrow SVG pointing right toward step 01 [mockup, also matches v2 ReferenceLayerDemo headline]
- Step labels [mockup]: `01 Pick references`, `02 AI decodes your taste`, `03 Multiple models generate`, `04 Refine and ship`

Layout: 4-step horizontal flow on the right side (left side is the heading column). Each step has a mono `01 / 02 / 03 / 04` eyebrow above its visual.

Step visuals:
1. **01 Pick references**: 2x2 grid of 4 reference stills sourced from `/v3/flow_01_refs.jpg` (single composite image)
2. **02 AI decodes your taste**: centered glowing green orb, `/v3/flow_02_orb.png` (transparent PNG over black panel), animated with existing `animate-glow-pulse`
3. **03 Multiple models generate**: 2x2 grid of 4 generated outputs `/v3/flow_03_generate.jpg`
4. **04 Refine and ship**: single wide cinematic still `/v3/flow_04_final.jpg` plus a small overlaid refinement panel listing `Color`, `Contrast`, `Motion`, `Mood` with mini-sliders

Between steps: lucide `ArrowRight`, `text-[var(--envato)]`, size 5.

Responsive:
- Desktop (lg+): heading column left, 4 steps in a horizontal row right
- Tablet (md): heading top, 2x2 grid of steps below
- Mobile: vertical stack, arrows rotate 90deg to face down

Funnel role: 20 sec stock-and-AI reframe per discovery Chain 03.

### 5.4 The world's creativity. Organised for yours. (`WorldCreativity.tsx`)

New section, no v2 equivalent.

Background: cream `var(--bg)`, full-bleed.

Copy:
- Heading: `The world's creativity.` line break `Organised for yours.` [mockup]
- Sub: `Millions of references from every culture, movement and moment. Curated, so you can create something new.` [mockup]

Layout:
- Two-column on desktop: text left (40%), art right (60%)
- Single column on mobile: text top, art below

Art: single editorial moodboard collage `/v3/moodboard_collage.jpg`, cropped so it bleeds slightly off the right edge.

No CTA.

Funnel role: 30 sec craft trust and "taste raw material" framing.

### 5.5 Made with Envato. Real work. Real people. (`RealWork.tsx`)

Background: dark `var(--hero)`.

Copy:
- Heading: `Made with Envato.` line break `Real work.` line break `Real people.` [mockup]
- Below heading: `See more stories` with right-arrow icon, link `#`

Layout: left header column + 5 cards in a horizontal row on the right.

Cards: 5 cards in a 5-up row at desktop, 2-up on tablet, 1.5-up scroll on mobile.

Creator data, real campaign creators per discovery research-facts §0:

| Card | Title | Creator handle |
|---|---|---|
| 1 | CAMPAIGN FILM | @cool_lookin_bug |
| 2 | BRAND IDENTITY | @Tapewarp.ai |
| 3 | MUSIC VIDEO | @JosephMartin |
| 4 | PRODUCT LAUNCH | @Rourke |
| 5 | SHORT FILM | @JamieFenn |

Each card:
- Aspect ratio 4:5
- Background image `/v3/story_0{1..5}.jpg`
- Bottom-left: small uppercase `CAMPAIGN FILM` etc, then `by @handle` underneath
- Bottom-right: small play-button overlay (lucide `Play` inside a 32px black/60 circle)
- No meme captions, no engagement counters, no tool pills. Editorial showcase, not social-proof reel.

Funnel role: 30 sec craft trust through real campaign creator names.

### 5.6 Learn how it's made. (`LearnHow.tsx`)

New section. No v2 equivalent. Placed between RealWork (dark) and LovedByBrands (cream). Reference: `/Users/uxersean/Desktop/Screenshot 2026-05-26 at 8.30.30 PM.png` ("OPTION A" mockup).

Background: cream `var(--bg)`, full-bleed. Right edge has a torn-paper decoration with handwritten margin pencil notes; a small glass orb sphere sits in the lower-right (echoes the pricing decoration). These are baked into a single right-edge image, `/v3/learn_decor_right.jpg`, anchored to the right border and clipped.

Copy:
- Heading: `Learn how it's made.` in `font-display`, large editorial scale. "made." gets a lime handwritten underline (CSS `border-bottom: 6px solid var(--envato)` with a slight rotate, OR a static SVG flourish committed at `public/v3/note_made_underline.svg`. The implementer picks whichever lands cleaner; if SVG, add it to §7.2.)
- Sub-caption (left column, below heading): `AI generation workflows taught by working creators.` line break `All free.`

Layout: left header column on desktop, 4 tutorial cards in a horizontal row on the right. At md: heading top, cards in a 2x2 grid. At sm: heading top, cards vertical stack.

Tutorial cards (4 cards, all data taken verbatim from the user spec):

| # | Tool badge | Badge style | Title (2 lines max) | Duration | Asset |
|---|---|---|---|---|---|
| 1 | ENVATO AI STACK | Lime fill, ink text | From Idea to Publish: Full AI Video Workflow | 45 min | `/v3/learn_01_aistack.jpg` |
| 2 | IMAGEGEN | Lime fill, ink text | AI Posters from Reference Images | 20 min | `/v3/learn_02_imagegen.jpg` |
| 3 | VIDEOGEN | Lime fill, ink text | Consistent AI Characters with Reference Sheets | 30 min | `/v3/learn_03_videogen.jpg` |
| 4 | VIDEOGEN + AFTER EFFECTS | Neutral fill (white/cream `var(--paper)`), ink text | Cinematic AI Video Effects on Any Budget | 35 min | `/v3/learn_04_aftereffects.jpg` |

Card chrome:
- Aspect ratio: 3:4 portrait
- Background image fills the card with rounded corners (`rounded-xl`)
- Top-left: rounded-rectangle tool badge, lime or neutral per the table above. Mono uppercase 10px, padding `px-2.5 py-1`. Includes a small lime caret tip on the lower-left edge of the badge (matches the screenshot's badge style).
- Center-bottom of thumbnail: round play-button (lucide `Play` inside a 40px black/85 circle). Rendered always at 70% opacity, transitions to 100% on hover. The user spec says "Hover state shows play icon overlay"; the mockup screenshot shows it visible on all 4 cards; we resolve by rendering at low opacity by default and full opacity on hover.
- Below the thumbnail (on the cream section background, not on the card): title in `font-display` 18px to 20px, two lines max, ink color, line-height 1.15
- Below title: row reading `45 min · FREE`. Duration in `font-mono` 12px, ink. `·` divider in `var(--muted-ink)`. `FREE` is a small lime pill: lime fill, ink text, `font-mono` 10px uppercase, `px-2 py-0.5 rounded-full`.

Section bottom, centered: `Browse all tutorials` with right-arrow icon. Outlined pill button: `border border-[var(--ink)] px-7 py-3.5 rounded-full text-[13.5px] font-semibold hover:bg-[var(--ink)] hover:text-white transition-colors`.

Funnel role: 40 sec educational trust signal. Closes the "AI literacy gap" surfaced by discovery research-facts §5 ("AI adoption is moving faster than AI literacy. The gap between enthusiasm and preparedness is where real opportunity exists. Our role is to provide the clarity and thoughtfully designed tools creatives need to navigate this transformation."). Envato is not just a tool, it teaches.

### 5.7 Loved by creators around the world. (`LovedByBrands.tsx`)

Background: cream `var(--bg)`.

Copy:
- Heading: `Loved by creators around the world.` [mockup]
- Sub: `And thousands more.` [mockup]

Layout: left header column + brand wordmark row on the right.

Brand logos: reuse v2 `SocialProof.tsx`'s real logo set: Miami Dolphins, KPMG, Audible, Warner Media, Pepsico, Unicef. Import from `@/components/icons/brand-logos`. No reels carousel.

Mockup shows fake placeholder wordmarks (AKQA, MSCHF, etc) that are not verified Envato customers. Per user direction: use v2's set.

Funnel role: 30 sec credibility.

### 5.8 Six tools. One canvas. Infinite combinations. (`SixTools.tsx`)

Background: dark `var(--hero)`.

Copy:
- Heading: `Six tools.` line break `One canvas.` line break `Infinite combinations.` [mockup]. "Infinite combinations." italic, `text-gradient-brand`
- Sub: `All the creative firepower you need, connected by your references.` [mockup, condensed from v2 MediumGrid intro]
- Bottom-left of heading column: `Explore all tools` with right-arrow icon, link `#`

Layout: left header column on desktop, 6 cards in a single horizontal row to the right at the widest breakpoint, wrapping to 3 columns at md, 2 columns at sm, 1 column at xs.

Cards (6 cards, names and pitches from v2 per rule 2.7, models from Envato public pages May 2026 per rule 2.8):

| Card | Tool name | Pitch | Models | Asset |
|---|---|---|---|---|
| 1 | VideoGen | Cinematic 4K shots in your taste range. | Veo 3.1, Kling 3.0, Seedance 2.0 | `/v3/tool_01_video.jpg` |
| 2 | ImageGen | Editorial stills from three references. | Flux.2, Seedream 4.5, Imagen 4 | `/v3/tool_02_image.jpg` |
| 3 | ImageEdit | Upscale and refine without losing the soul. | Magnific, Nano Banana Pro | `/v3/tool_03_edit.jpg` |
| 4 | MusicGen | Score that fits your edit, not the algorithm. | ElevenLabs Music, Minimax 2.5, Mureka v8 | `/v3/tool_04_music.jpg` |
| 5 | VoiceGen | Studio-grade voiceovers, any tone. | ElevenLabs | `/v3/tool_05_voice.jpg` |
| 6 | MockupGen | Product shots without a shoot. | Envato Studio | `/v3/tool_06_mockup.jpg` |

Model pill count rule: up to 3 verified models per card. If fewer real models are publicly listed for that tool, render fewer pills. ImageEdit renders 2 pills, VoiceGen and MockupGen render 1 pill each. No padding with fictional models.

Model lineup sources, all `elements.envato.com` May 2026:
- VideoGen: `/learn/videogen-models`, `/lp/ai-videogen/`. Eleven models including Veo 3.1, Kling, Seedance 2.0, Hailuo 02, Runway, Ray 3.14
- ImageGen: `/learn/envato-imagegen-ai-models`. Flux.2, Flux.2 (max), Seedream 4.5, Nano Banana, Nano Banana Pro, gpt-image-1, Imagen 4
- ImageEdit: `/lp/ai-stack/`. Magnific upscale and refine flow
- MusicGen: `/learn/whats-new-at-envato`. ElevenLabs Music, Minimax Music 2.5, Mureka v7.6/v8/o2
- VoiceGen: ElevenLabs
- MockupGen: Envato Studio (proprietary)

We ship up to 3 real model names per card, exactly as listed in the table above. The pills carry only real model names; no fictional pairings. Where the verified list yields fewer than 3 (ImageEdit 2, VoiceGen 1, MockupGen 1), fewer pills are rendered.

Card chrome:
- Background image fills the card
- Gradient scrim from bottom 70% black to top 0%
- Top-left: small sparkle icon (lucide `Sparkles`) + uppercase tool name in mono 10px
- Bottom-left: pitch in `font-display` 22px, then a row of up to 3 model pills (mono 10px, white border, white/5 fill). The row renders exactly the number of pills defined in the table for that card.
- Bottom-right: lime arrow icon (`ArrowUpRight`) that translates on hover

No MCP banner in v3. MCP integration moves out of the homepage per mockup.

Funnel role: 45 sec product-breadth signal under the discovery's "Six modalities, one canvas" framing.

### 5.9 Simple pricing. Serious value. (`PricingV3.tsx`)

Background: cream `var(--bg)`.

Copy:
- Heading (left column): `Simple pricing.` line break `Serious value.` [mockup]
- Decoration: glass-orb still image `/v3/glass_orb.png` bottom-left of heading column
- Toggle (top-right of tier row): pill switch `Monthly` / `Annual` with `Save up to 30%` hint below

Tier cards (3-up row on desktop, stack on mobile), data from discovery research-facts §1:

| Tier | Monthly price | Pitch [v2] | Features (in order) | CTA [v2] | Recommended |
|---|---|---|---|---|---|
| Core | A$16.50 | For all creators. | 10 AI generations / month, Unlimited downloads, Lifetime commercial licence | Start with Core | no |
| Plus | A$39 | For active creators. | 100 AI generations / month, Everything in Core, Priority support | Try Plus, 7 Days Free | yes |
| Ultimate | A$109 | For teams and agencies. | Unlimited AI generations, Everything in Plus, Team management | Go Ultimate | no |

Annual toggle math: monthly price * 12 * 0.7, rounded to 2 decimals.
- Core annual: A$138.60 / year displayed as headline, sub-line `A$11.55 / mo billed annually`
- Plus annual: A$327.60 / year displayed as headline, sub-line `A$27.30 / mo billed annually`
- Ultimate annual: A$915.60 / year displayed as headline, sub-line `A$76.30 / mo billed annually`

Card chrome:
- Plus card: dark `var(--ink)` background, white text, lime "Most popular" pill above the card top edge
- Core and Ultimate cards: paper background, ink text, line border that turns ink on hover
- Check icons in front of each feature line, lime in dark card, envato-deep in light cards
- CTA: full-width pill button at the card bottom, lime fill on Plus, lime outline on Core and Ultimate

Below cards: small disclaimer `Price-locked 12 months · Lifetime license · Cancel anytime.` [mockup]

FAQ block (abbreviated per user direction):
- Two items only, from v2 `Pricing.tsx` `FAQS` array:
  1. `Can I use Envato assets for client work?`
  2. `Are AI generations commercially safe?`
- Default state: first item open. Same collapsing accordion UI as v2.
- Link below the FAQ: `Visit the Help Centre` (right arrow icon).

Funnel role: 60 sec commitment step.

### 5.10 Stop describing taste. Start showing it. (`FinalCtaV3.tsx`)

Background: dark `var(--hero)`, collage edge image `/v3/finalcta_v3_bg.jpg` along the bottom.

Copy:
- Headline: `Stop describing taste.` line break `Start showing it.` [mockup]. "Start showing it." italic, `text-gradient-brand`
- Disclaimer line: `Price-locked 12 months · Lifetime license · Cancel anytime.` [mockup]
- Primary CTA: `Start Free Trial`, links to `#pricing`
- Secondary CTA: `See plans`, links to `#pricing`

Layout: single column, centered text, large headline, CTAs on a single row.

Funnel role: closes the page with the brief's "anti-generic" thesis.

### 5.11 Nav and Footer

Both reused from v2 unchanged. Discovery deprioritizes category routing in the hero but acquisition surfaces typically retain a header nav for return visitors and SEO. Documented as accepted tension, not a blocker.

## 6. Implementation passes (Codex CLI does the code)

Five sequential passes. Each pass has a self-contained acceptance test that does not require running any later pass. Earlier passes must complete before later ones start, but verification of an earlier pass does not depend on completing a later one.

**Pass A. Routing scaffold.** Goal: `/v3` route renders ten sections in spec order with placeholder content.
- Create `src/app/v3/page.tsx` importing v2 `Nav`, v2 `Footer`, `Reveal`, and ten new v3 section components.
- Create ten stub components under `src/components/sections/v3/`. Each stub renders its section background color plus a single temporary headline node.
- Acceptance for Pass A in isolation:
  - `npm run build` exits 0.
  - `npm run dev`, open `/v3`, see ten vertical colored blocks in the order Hero, KpiPill, ThreeRefsFlow, WorldCreativity, RealWork, LearnHow, LovedByBrands, SixTools, PricingV3, FinalCtaV3.
  - Browser console shows zero errors.
  - `/` still renders v2 unchanged.

**Pass B. Markup, copy, layout (no real assets).** Goal: every section's canonical copy from §5 is in place, responsive layout is correct, image slots show neutral gray `bg-[var(--ink)]` blocks where artwork will land.
- For every `<Image>` slot referenced in §5, render either a solid `bg-[var(--ink)]` div placeholder at the correct aspect ratio, or use a `src` that points to `/v3/*` if the file already exists. Do not call `<Image>` with paths that 404; gray placeholders prevent broken-image warnings.
- Implement the Pricing monthly/annual toggle to swap to the strings in §5.8.
- Implement the orb pulse with `animate-glow-pulse`.
- Acceptance for Pass B in isolation:
  - `npm run build` exits 0.
  - Open `/v3` at 1440x900: layout structurally matches the mockup composition. Implementer compares against the mockup by section. Visual artwork is intentionally absent in this pass.
  - Pricing toggle clicked twice returns to the original prices.
  - No 404s in the network tab for `/v3` page assets.

**Pass C. Asset generation.** Goal: every artwork file referenced by Pass B exists at the correct path.
- Generate 24 images via Codex CLI, one Bash call per asset, output written to `public/v3/`. Filenames and asset descriptions in §7.1; the implementer writes the actual image-generation prompts from those descriptions.
- Author 2 SVG handwritten note files by hand and commit them to `public/v3/`. Content in §7.2.
- Replace the gray placeholders in Pass B markup with the real `<Image>` calls.
- Acceptance for Pass C in isolation:
  - `ls public/v3/` lists exactly 26 files matching the names in §7.
  - `npm run build` exits 0.
  - Open `/v3`: no broken-image icons. Hero shows the collage. Each tool card shows its tool image. Moodboard shows the collage.

**Pass D. Motion polish.** Goal: scroll reveals on every section, hero rise-in animation, orb pulse confirmed.
- Wrap each v3 section in `<Reveal>` inside the page composition.
- Confirm hero `animate-rise-in` runs on first paint.
- Confirm orb continues to pulse.
- Acceptance for Pass D in isolation:
  - Scroll from top to bottom of `/v3`: each section fades or rises into view as it enters viewport.
  - Hero headline staggers in on initial load.
  - Orb visibly pulses.

**Pass E. Cross-viewport verification.** Goal: layout holds at desktop and mobile.
- Capture `/v3` at 1440x900 and 390x844 in Chrome.
- Diff each capture against the mockup composition per section. Note any structural deviation.
- Acceptance for Pass E in isolation:
  - `npm run build` exits 0.
  - Both captures saved. Each section recognizable side-by-side against the mockup at desktop. Mobile layout has no overflow and no text smaller than 11px.
  - v2 at `/` still renders unchanged.

## 7. Asset checklist (24 generated + 2 authored SVG)

All assets in `public/v3/`. Generated by Codex CLI per the brief style: editorial collage in the Saul Bass / Sagmeister lineage, torn paper edges, halftone print textures, scattered nature elements (butterflies, cactus, flowers), occasional bold-color geometric accents (red sun disc, lime accent), warm cream and black palette.

### 7.1 Generated images

| File | Section | Aspect | Description |
|---|---|---|---|
| `hero_v3_main.jpg` | §5.1 Hero | 16:9 | Editorial collage portrait. Photographer holding a vintage camera, large purple sunglasses, halftone red sun disc behind the head, halftone building texture, scattered butterflies, single cactus and flower, torn-paper edges. |
| `flow_01_refs.jpg` | §5.3 step 01 | 1:1 | 2x2 grid of 4 reference stills: mountain peak, gothic arch, fern close-up, modernist building facade. Unified cinematic grade. |
| `flow_02_orb.png` | §5.3 step 02 | 1:1 | Glowing green AI orb on pure black, ring of particles. Transparent PNG. |
| `flow_03_generate.jpg` | §5.3 step 03 | 1:1 | 2x2 grid of 4 generated outputs: surfer in wave, classical statue, turntable, modernist building. Same grade as flow_01_refs. |
| `flow_04_final.jpg` | §5.3 step 04 | 16:10 | Single wide cinematic still: surfer carving a wave at golden hour. |
| `moodboard_collage.jpg` | §5.4 | 4:3 | Editorial moodboard pile: snowy peak, cathedral spire, large eye close-up, neon butterfly, single pink flower, cactus, polaroid scraps, color swatches. Torn paper, layered. |
| `story_01.jpg` | §5.5 card 1 | 4:5 | CAMPAIGN FILM still: futuristic cityscape at sunset, film grade. |
| `story_02.jpg` | §5.5 card 2 | 4:5 | BRAND IDENTITY still: single backlit butterfly on dark background. |
| `story_03.jpg` | §5.5 card 3 | 4:5 | MUSIC VIDEO still: performer figure under magenta stage light. |
| `story_04.jpg` | §5.5 card 4 | 4:5 | PRODUCT LAUNCH still: perfume bottle in soft amber light. |
| `story_05.jpg` | §5.5 card 5 | 4:5 | SHORT FILM still: walking figure in foggy street, anamorphic. |
| `learn_01_aistack.jpg` | §5.6 card 1 | 3:4 | Cinematic AI video still: lone figure with umbrella on a neon-lit rainy street. Final-output look (not a workflow screen). |
| `learn_02_imagegen.jpg` | §5.6 card 2 | 3:4 | AI-generated editorial poster ("NEO ROMANCE" or similar), with a reference grid of small mood images visible behind the hero poster. Workflow capture aesthetic. |
| `learn_03_videogen.jpg` | §5.6 card 3 | 3:4 | 4-up grid of the same AI character across different scenes (mountain, sunset, portrait, ridge). Hand-scribbled "Same character" annotation overlay in lime. |
| `learn_04_aftereffects.jpg` | §5.6 card 4 | 3:4 | Cinematic AI-generated car shot with red light streaks (motion blur), beside a small After-Effects timeline / color-wheel UI strip at the bottom of the frame. |
| `learn_decor_right.jpg` | §5.6 right edge | 9:16 | Right-edge decoration: torn paper edges, small handwritten pencil margin notes, a small glass-orb sphere in the lower third. Cream and ink palette. |
| `tool_01_video.jpg` | §5.8 card 1 | 4:5 | Cyan wave wall with surfer, painterly. |
| `tool_02_image.jpg` | §5.8 card 2 | 4:5 | Close-up monarch butterfly on torn paper. |
| `tool_03_edit.jpg` | §5.8 card 3 | 4:5 | Pink romanesque arch interior with soft window light. |
| `tool_04_music.jpg` | §5.8 card 4 | 4:5 | Macro of a turbine, metallic and reflective. |
| `tool_05_voice.jpg` | §5.8 card 5 | 4:5 | Vintage condenser microphone on dark velvet. |
| `tool_06_mockup.jpg` | §5.8 card 6 | 4:5 | Classical statue with rising red moon behind. |
| `glass_orb.png` | §5.9 | 1:1 | Single transparent glass sphere on cream background, photoreal, transparent PNG. |
| `finalcta_v3_bg.jpg` | §5.10 | 21:9 | Collage edge with peeking camera, butterflies, cactus, torn paper, wide letterbox. |

### 7.2 Authored SVG notes

| File | Section | Content |
|---|---|---|
| `note_taste.svg` | §5.1 hero | Handwritten "Your taste, not the algorithm." with star ornament, lime stroke on cream, inline SVG path. |
| `note_refs.svg` | §5.3 | Handwritten "Your references become the AI's instructions." with curved lime arrow that points right, inline SVG path. |

## 8. Out of scope

- Replacing v2 at `/`. v3 lives at `/v3` until the user says otherwise.
- Internationalization, translated copy.
- Real Stripe pricing wire-up, real auth.
- Editing any v2 component file. If a v2 file needs a tweak, stop and ask.
- Lighthouse perf targets beyond a clean console.
- The Creative Flow page at `/creative-flow`. Already shipped, untouched.

## 9. Open items the user has decided

These are recorded so a future reviewer doesn't re-litigate:

1. FAQ: keep, abbreviated to 2 items under pricing. (User chose abbreviated keep.)
2. Creator handles: real Creative Flow Activated campaign creators (@cool_lookin_bug, @Tapewarp.ai, @JosephMartin, @Rourke, @JamieFenn). (User chose real.)
3. MCP banner: removed from the homepage per mockup. (User chose remove.)
4. Model pills: keep with real, verified Envato models per Envato May 2026 public pages. (User chose research-based real models.)

## 10. v2 vs discovery vs mockup conflicts (resolved)

This table records every fact where two sources disagreed and the source-of-truth rule in §2 produced a resolution. Implementer pastes the "v3 value" column.

| # | Field | v2 value | Discovery value | Mockup value | Source winner | v3 value |
|---|---|---|---|---|---|---|
| 1 | Plus tier monthly price | A$33 | $39 (Feb 25 2026 launch) | A$39 | Discovery numerical | A$39 |
| 2 | Core tier feature list | Unlimited downloads, 100M+ premium assets, Lifetime commercial licence | 10 AI gen / mo, unlimited downloads, lifetime license | 10 AI generations / month, Unlimited assets, Lifetime commercial license | Discovery (rule 2.3) | 10 AI generations / month, Unlimited downloads, Lifetime commercial licence |
| 3 | Plus tier feature list | Everything in Core, 100 AI generations / month, Priority support | 100 AI gen / mo, everything in Core, priority support | 100 AI generations / month, Everything in Core, Priority support | Discovery (rule 2.3) | 100 AI generations / month, Everything in Core, Priority support |
| 4 | Hero sub-headline | Pick a few references. We match the rest to your taste. You ship. | Pick references. AI generates with your taste in the loop. | Pick a few references. AI generates with your taste in the loop. | Mockup (matches discovery thesis) | Pick a few references. AI generates with your taste in the loop. |
| 5 | Final CTA headline | Your next project starts with a reference. | (not specified) | Stop describing taste. Start showing it. | Mockup | Stop describing taste. Start showing it. |
| 6 | ReferenceLayer / ThreeRefsFlow heading | Your references become the AI's instructions. | (Chain 03: 4-step strip) | From three references, one creation. (with the v2 line as handwritten note) | Mockup | From three references, one creation. + the v2 line as handwritten annotation |
| 7 | Creator handles | Fictitious @michaelfbarker, @_alexpillow, @saraplaysrec, @sonic.studio, @pixelperfect, @marinerstudio | Real @cool_lookin_bug, @Tapewarp.ai, @JosephMartin, @Rourke, @JamieFenn | Different fictitious set | Discovery (user-confirmed) | @cool_lookin_bug, @Tapewarp.ai, @JosephMartin, @Rourke, @JamieFenn |
| 8 | Brand wordmark logos | Miami Dolphins, KPMG, Audible, Warner Media, Pepsico, Unicef | (not specified) | Fake placeholder wordmarks | v2 (user-confirmed) | v2 set unchanged |
| 9 | Model pills on tool cards | Mixed real and speculative (Sora 2, Imagen 3, Flux Pro) | Decision board says drop model name dropping; research-facts says Envato keeps the lineup deliberately undisclosed | Mockup omits pills entirely | Envato May 2026 public pages (rule 2.8, user-chosen real models) | Real verified models per §5.7 table. Up to 3 per card; fewer if fewer real models exist. ImageEdit 2, VoiceGen 1, MockupGen 1. |
| 10 | MCP banner | Present in MediumGrid | Real Nov 2025 product per research-facts | Omitted from mockup | Mockup (user-confirmed) | Removed from homepage |
| 11 | Pricing FAQ | 4 items | (not specified) | Omitted | v2 abbreviated (user-confirmed) | 2 items, first two from v2 FAQS array |
| 12 | Sub-headline for SocialProof / LovedByBrands | Made with Envato, by real creators (with reels grid) | (not specified) | Loved by creators around the world. And thousands more. (logos only) | Mockup | Loved by creators around the world. + And thousands more. + v2 logo set |
| 13 | KPI labels | Assets, Creators, Licensed, Downloads, Subscription | (not specified) | Assets & templates, Creators, Commercial license, Downloads, Creative platform | v2 (mockup labels not contradicted by discovery, v2 is shorter and matches surface convention) | v2 labels |
| 14 | Tool list | VideoGen, ImageGen, ImageEdit, MusicGen, VoiceGen, MockupGen (6 tools) | Discovery research-facts lists 7 (adds GraphicsGen) | Mockup shows 6 cards under "Six tools." framing | Mockup composition (rule 2.9) for the 6-count + v2 (rule 2.7) for the names | 6 tools per the v2 set, matching the mockup's "Six tools" heading. The 7th from discovery (GraphicsGen) is intentionally out of scope per mockup. |
| 15 | Below-fold structure | Linear: refs demo, tool grid, creator grid, social proof, pricing, final CTA | Decision board Chain 06 calls for cinematic feature sections, catalog browse demoted | Mockup matches the discovery direction | Mockup (matches discovery) | Mockup order |
| 16 | Header Nav | Includes category routing + Sign In above the fold | Decision board deprioritizes category routing in the hero | Mockup retains a header with the same pattern | v2 (mockup retains; acquisition surface convention) | v2 Nav unchanged. Documented as accepted tension. |

## 11. v2 files that are NOT touched

This v3 work creates new files only. The following v2 files stay untouched:

- `src/app/page.tsx`
- `src/components/sections/HeroV2.tsx`
- `src/components/sections/KpiBand.tsx`
- `src/components/sections/ReferenceLayerDemo.tsx`
- `src/components/sections/MediumGrid.tsx`
- `src/components/sections/CreatorGrid.tsx`
- `src/components/sections/SocialProof.tsx`
- `src/components/sections/Pricing.tsx`
- `src/components/sections/FinalCta.tsx`
- `src/components/sections/Nav.tsx` (imported by v3 page)
- `src/components/sections/Footer.tsx` (imported by v3 page)
- `src/components/Reveal.tsx` (imported by v3 page)
- `src/components/icons/brand-logos.tsx` (imported by `LovedByBrands.tsx`)
- `src/app/globals.css`
- `src/app/creative-flow/*` (the considered-path scrollytelling page)

## 12. Definition of done

- `/v3` route renders ten sections in spec order with the canonical copy in §5.
- All 24 generated images and 2 authored SVGs exist in `public/v3/`.
- Pricing toggle swaps to the exact annual strings in §5.8.
- `npm run build` passes with zero TS errors and no missing-image warnings.
- Open `/v3` at 1440x900 and 390x844 in Chrome; layout matches mockup at both sizes.
- v2 at `/` still renders unchanged.
- No em dashes in any copy string or comment introduced by this work.
